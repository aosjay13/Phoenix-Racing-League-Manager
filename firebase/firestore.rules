rules_version = '2';

// All reads/writes go through the Next.js API routes using the Admin SDK,
// which bypasses these rules. Lock the client SDK out entirely.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
